# 怀仁 AI 中台 — Python 依赖
# 运行环境建议：Python 3.10+（3.9 多数场景可用）
# 部署另需配置 .env（OFOX_API_KEY、OSS、会话密钥等），见 app/config.py

# ----- Web 服务 -----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
itsdangerous>=2.2.0
python-dotenv>=1.0.0

# ----- HTTP 客户端（上游 OFOX / 模型网关）-----
requests>=2.31.0

# ----- MySQL / ORM -----
SQLAlchemy>=2.0.36
PyMySQL>=1.1.1
Werkzeug>=3.0.4

# ----- 阿里云 OSS（聊天记录 index + conversations）-----
oss2>=2.18.0

# ----- 附件正文抽取（PDF / Word / Excel）-----
pypdf>=5.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.2
reportlab>=4.2.0

# ----- 图片处理（生图尺寸、缩放等，PIL）-----
Pillow>=10.4.0

# 说明：
# - pydantic / starlette / websockets 由 fastapi / uvicorn[standard] 自动安装，无需单独列出
# - 旧版 .doc 抽取依赖系统命令 antiword/catdoc（非 pip），未安装时上传 .doc 会有提示文案
